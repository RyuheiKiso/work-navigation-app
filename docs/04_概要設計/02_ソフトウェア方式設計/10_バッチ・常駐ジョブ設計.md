# 10 バッチ・常駐ジョブ設計

本章の責務は、BAT 識別子（BAT-001〜010）で管理するバッチ・常駐ジョブの実行基盤・スケジューリング・冪等性設計を確定することである。

---

## 1. バッチ実行基盤

### 1-1. Rust バックエンドの常駐 tokio task

長時間稼働するジョブ（Outbox Consumer・WAL アーカイブ等）は Rust の tokio 非同期タスクとして実装する。

```rust
// main.rs での起動
#[tokio::main]
async fn main() {
    // API サーバー
    let api_handle = tokio::spawn(serve_api(app_state.clone()));
    
    // Outbox Consumer（BAT-002）
    let outbox_handle = tokio::spawn(
        outbox_consumer::run(app_state.clone())
    );
    
    // ハッシュチェーン検証（BAT-001・週次 cron）
    let hash_verify_handle = tokio::spawn(
        hash_chain_verifier::run(app_state.clone())
    );
    
    tokio::select! {
        _ = api_handle => {},
        _ = outbox_handle => {},
        _ = hash_verify_handle => {},
    }
}
```

### 1-2. cron スケジューリング

cron 式で実行するジョブは `tokio-cron-scheduler` を使用する。または Docker Compose 内の別コンテナ（`wnav-scheduler`）で管理する。

---

## 2. BAT カタログ詳細

| BAT-ID | 名称 | 実行契機 | 冪等性 | 失敗時 |
|---|---|---|---|---|
| BAT-001 | hash_chain_verifier | 週次 cron（月曜 02:00）| ✅（同一週は skip）| ERR-DB-003 + LOG-007 |
| BAT-002 | outbox_dispatcher | 常駐（60s ループ）| ✅（idempotency_key）| retry / DLQ |
| BAT-003 | master_sync_puller | cron（毎 60 分）| ✅（as_of タイムスタンプ）| LOG + 次回再試行 |
| BAT-004 | pii_anonymizer | 日次 cron（03:00）| ✅（anonymized_at チェック）| LOG + 手動確認 |
| BAT-005 | backup_pg_dump | 日次 cron（01:00）| ✅（ファイル存在確認）| LOG + アラート |
| BAT-006 | backup_wal_archive | 常駐（WAL ストリーミング）| ✅（WAL LSN 管理）| PITR フォールバック |
| BAT-007 | document_hash_recorder | 帳票生成後トリガ | ✅（report_id チェック）| LOG + 手動再実行 |
| BAT-008 | webhook_retry_scheduler | cron（毎 1 分）| ✅（idempotency_key）| DLQ 移行 |
| BAT-009 | cert_expiry_notifier | 日次 cron（09:00）| ✅（通知済みフラグ）| ログのみ |
| BAT-010 | jwt_key_rotator | cron（90 日ごと）| ✅（KEY-001 版数チェック）| アラート + 手動確認 |

---

## 3. 冪等性設計の統一パターン

全バッチの冪等性は以下のいずれかで担保する。

| パターン | 適用バッチ | 実装例 |
|---|---|---|
| 実行記録確認 | BAT-001/005/007/009 | `IF NOT EXISTS (SELECT 1 FROM batch_logs WHERE batch_id = 'BAT-001' AND period = 'week-2026-20')` |
| タイムスタンプ比較 | BAT-003/004 | `WHERE last_sync_at < NOW() - INTERVAL '60 minutes'` |
| Idempotency Key | BAT-002/008 | `outbox_events.idempotency_key` の重複確認 |
| WAL LSN | BAT-006 | `pg_current_wal_lsn()` との比較 |

---

## 4. ジョブ実行失敗時の自動回復方針

### 4-1. 再試行ポリシー（BAT 種別ごと）

| BAT-ID | 自動再試行 | 最大試行 | 人手介入条件 |
|---|---|---|---|
| BAT-001 | なし（週次再実行）| 1 | 3 週連続失敗 |
| BAT-002 | 指数バックオフ | CFG-002（5）| DLQ 移行後 |
| BAT-003 | 次回 cron 実行 | — | 24h 以上失敗継続 |
| BAT-004 | 翌日 cron 実行 | — | — |
| BAT-005 | 6h 後リトライ | 3 | 復旧不可時 |
| BAT-006 | 自動（WAL daemon）| — | WAL 容量超過時 |
| BAT-007 | 手動（API-ops-002）| — | IT 担当判断 |
| BAT-008 | 指数バックオフ | 5 | DLQ 移行後 |

---

**本節で確定した方針**
- **BAT-001〜010 の全バッチを確定し、実行契機・冪等性担保方式・失敗時処理を設計命題として確定した。**
- **長時間稼働ジョブ（BAT-002 Outbox Consumer・BAT-006 WAL）は Rust tokio task として API サーバーと同一プロセスで起動し、tokio::select! でライフサイクルを管理する。**
- **全バッチは冪等性を担保し、二重実行が発生しても安全に動作する設計を確定した。**

---

## 参照業界分析

### 必須
- [`90_業界分析/27_オフライン同期とデータ整合性.md`](../../90_業界分析/27_オフライン同期とデータ整合性.md)

### 関連
- [`90_業界分析/06_品質管理とトレーサビリティ.md`](../../90_業界分析/06_品質管理とトレーサビリティ.md)
