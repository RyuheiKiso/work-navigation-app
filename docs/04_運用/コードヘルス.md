# コードヘルス月次スナップショット

> 対応 §: ロードマップ §9.6 §22.1 §22.4 §29 R-001
> 対象読者: メンテナ、月次レビュア
> 改訂サイクル: 月次（測定値）／§22.1 半期（指標自体）

§9.6 で固定された 14 指標の月次値を保持する。本書は §29 R-001（バーンアウト）兆候として活用される。

## 1. 月次値表（直近）

| 指標 | 目標 | 計測手段 | 2026-05 値 | 状態 |
| --- | --- | --- | --- | --- |
| ファイル行数 | 500 行以下 | `scripts/lint-file-size.sh`（CI fail） | NA（コード未実装） | — |
| 関数当たり行数 | 50 行以下（中央値） | `tokei` ＋ AST 解析 | NA | — |
| 循環的複雑度（McCabe） | 各関数 ≤ 10 | Rust: `cargo geiger` 系／TS: `eslint complexity` | NA | — |
| 認知的複雑度（Sonar） | 各関数 ≤ 15 | `eslint sonarjs/cognitive-complexity` | NA | — |
| パブリック API rustdoc／TSDoc | 100% | `cargo doc --no-deps` warning ゼロ | NA | — |
| ドメイン層単体カバレッジ | ≥ 90%（§21 注 7） | `cargo llvm-cov`／`vitest --coverage` | NA | — |
| ミューテーションスコア（ドメイン） | ≥ 80%（§13.4.1） | `cargo-mutants` | NA | — |
| 依存方向違反 | 0 件 | `cargo modules`／`dependency-cruiser` | NA | — |
| TODO／FIXME 経過日数 | 90 日以内に解消 | CI `scripts/aging-todo.sh` | NA | — |
| デッドコード | 0 件（CI fail） | `cargo udeps`／`ts-prune` | NA | — |
| 重複コード | ≤ 5% | `jscpd`／`cargo-similar` | NA | — |
| ライセンス汚染 | 0 件 | `cargo deny`／`license-checker` | NA | — |
| 静的解析 warning | 0 件（denied） | `clippy --deny warnings`／`eslint --max-warnings 0` | NA | — |
| 1 PR 差分 | ≤ 500 行（§9.4） | GitHub Actions サイズチェック | NA | — |
| 各行コメント率（CLAUDE.md 規定） | 100%（実コード行に対し直前行コメント） | `scripts/lint-line-comments.sh` | NA | — |

## 2. 月次トレンド

| 月 | 主要変動 |
| --- | --- |
| 2026-05 | 初版確定。コード未実装のため全指標 NA |

## 3. ホワイトリスト（§9.6）

例外的にしきい値を超えるファイル／関数は ADR（§9.4）で根拠を残し、CI のホワイトリスト `.codehealth-allow` に入れる。本月時点でホワイトリスト 0 件。

| 件 | 対象 | しきい値 | 例外理由 ADR |
| --- | --- | --- | --- |
| — | — | — | — |

ホワイトリスト件数自体も §22.1 で監視（増加トレンドは §2 沈黙の妥協の兆候とみなす）。

## 4. バーンアウト兆候監視（§29 R-001）

- PR 滞留件数（中央値）: 0 件（公開前）
- レビュー所要時間（中央値）: NA
- Issue 閉鎖速度: NA
- 公開後は §19.5.2 メンテナ可用時間予算と本指標を突き合わせ、悪化トレンド検出時に副メンテナ確保（§19.4）または機能撤退（§22.3）を検討する。

## 5. 受入観点（§9.6）

- 全指標が CI ジョブとして登録されていること（コード初期実装後）。
- 月次スナップショットが過去 12 ヶ月分保持されていること。
- §29 R-001（バーンアウト）兆候として活用されていること。
- 指標悪化が 2 期連続で確認された区分は §22.4 是正フロー起動。
