# ミューテーションレポート（テンプレート）

> 対応 §: ロードマップ §13.4.1 §9.6 §22.4
> 対象読者: メンテナ、ドメイン層実装者
> 改訂サイクル: 月次（重い処理のため）

§13.4.1 ミューテーションテスティングの月次結果を本テンプレートに従って `docs/04_運用/mutation-report-<YYYY-MM>.md` に蓄積する。本書は雛形であり、実値を持たない。

## 1. 計測対象

| 対象 | ツール | 目標スコア |
| --- | --- | --- |
| Rust ドメイン層 | `cargo-mutants` | ≥ 80% |
| TypeScript（フロント） | `stryker-mutator` | ≥ 70% |

## 2. サマリ（記入例）

| 月 | Rust ドメイン層 スコア | TypeScript スコア | 合否 |
| --- | --- | --- | --- |
| YYYY-MM | xx.x% | xx.x% | OK／NG |

## 3. 検出されたミューテーション（生存していたもの）

| 件 | ファイル：行 | ミューテーション種別 | 検出されなかった理由（仮説） | 対応 |
| --- | --- | --- | --- | --- |
| 1 | `path/to/file.rs:NN` | `replace_arithmetic_op` | テストが境界値を網羅していない | テスト追加（PR #NN） |

## 4. テスト追加の概要

- ミューテーション生存箇所に対してテストを追加し、再計測でスコアが目標達成することを確認する。
- 性質ベーステスト（`proptest`）の追加が有効な場合は積極的に採用。

## 5. §22.4 是正フローへの連結

- 目標未達月は §9.6 コードヘルス指標悪化として §22.4 是正フローを起動する。
- 是正完了は次月レポートに「対応完了済 PR #NN」として記載する。

## 6. 受入観点（§13.4.1）

- 月次でレポートが本テンプレートに従って作成されていること。
- 目標未達月のすべてに対応 Issue／PR が紐付き、再計測での回復が記録されていること。
- §9.6 コードヘルス指標との整合が確認されていること。
