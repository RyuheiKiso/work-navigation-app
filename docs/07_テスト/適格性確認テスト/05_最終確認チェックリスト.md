# 05 最終確認チェックリスト

本章は ver1.0.0 リリース前に確認すべき全項目を RGATE-001〜006 + UAT 合格の観点で確定する。
全項目にチェックが入った状態でのみリリース判定（`08_リリース判定とサインオフ.md`）に進む。

---

## RGATE-001: コード品質

### Rust（バックエンド）

- [ ] Clippy 警告 0 件
  ```bash
  cargo clippy --all-targets --all-features -- -D warnings
  # → warning: 0 件
  ```

- [ ] `cargo test` 全件 Pass（バックエンド単体テスト）
  ```bash
  cargo test --all
  # → test result: ok. X passed; 0 failed
  ```

- [ ] `cargo fmt --check` フォーマット違反 0 件
  ```bash
  cargo fmt --all -- --check
  # → exit code 0
  ```

### React / React Native（フロントエンド）

- [ ] ESLint エラー 0 件（警告も 0 件）
  ```bash
  pnpm eslint --max-warnings 0
  # → exit code 0
  ```

- [ ] TypeScript 型エラー 0 件
  ```bash
  pnpm tsc --noEmit
  # → exit code 0
  ```

- [ ] Prettier フォーマット違反 0 件
  ```bash
  pnpm prettier --check "**/*.{ts,tsx}"
  # → exit code 0
  ```

---

## RGATE-002: テストカバレッジ

### Rust カバレッジ

- [ ] 行カバレッジ ≥ 80%
  ```bash
  cargo llvm-cov --all-features --html
  # → Line Coverage: XX.X% (≥ 80%)
  ```

- [ ] ドメイン層（`domain/`）行カバレッジ ≥ 90%

### React / React Native カバレッジ

- [ ] 行カバレッジ ≥ 80%
  ```bash
  pnpm test --coverage
  # → Lines: XX% (≥ 80%)
  ```

### E2E・統合テスト

- [ ] E2E UC カバレッジ ≥ 95%（≥ 21/22 件 Pass）
- [ ] 最優先 6 件（TST-e2e-003/009/011/016/019/020）全 Pass
- [ ] BR-BUS 統合テスト 100%（45/45 件 Pass）

---

## RGATE-003: セキュリティ

- [ ] cargo audit CVSS HIGH/CRITICAL ゼロ
  ```bash
  cargo audit
  # → 0 vulnerabilities (HIGH/CRITICAL)
  ```

- [ ] npm/pnpm audit CVSS HIGH/CRITICAL ゼロ
  ```bash
  pnpm audit --audit-level high
  # → No high/critical vulnerabilities
  ```

- [ ] OWASP ZAP HIGH アラート 0 件（TST-sec-005）
  ```bash
  docker run -t ghcr.io/zaproxy/zaproxy:stable \
    zap-baseline.py -t http://api-server:8080 -r zap-report.html -I
  # → HIGH: 0
  ```

- [ ] RBAC 検証（TST-sec-001〜003）全 Pass
- [ ] JWT 期限切れ 401 確認（TST-sec-004）
- [ ] ブルートフォース保護 HTTP 423 確認（TST-sec-007〜008）
- [ ] HMAC 改ざん/欠落 HTTP 401 確認（TST-sec-009〜010）
- [ ] 個人別ランキング拒否 HTTP 403 確認（TST-sec-006 / BR-BUS-029）

---

## RGATE-004: API 準拠

- [ ] Schemathesis スキーマ準拠率 100%
  ```bash
  schemathesis run http://localhost:8080/api/v1/openapi.json \
    --checks all --max-examples 100
  # → 0 failures
  ```

---

## RGATE-005: 性能

- [ ] k6 P95 ≤ 2000ms（50 VU × 5min / TST-perf-001）
- [ ] k6 5xx エラーレート 0%（TST-perf-001/002）
- [ ] constant-arrival-rate 100rps × 60s 安定（TST-perf-002）
- [ ] Criterion ハッシュチェーン 100 万件 < 60s（TST-perf-003）
- [ ] Outbox 再接続後 ≤ 5s 全件同期（TST-perf-004）
- [ ] 1000 ステップ RP-001 生成 < 10s（TST-perf-005）

---

## RGATE-006: DTM 完全性

- [ ] `scripts/check-dtm-m5.sh` 未対応 NFR 0 件
  ```bash
  bash scripts/check-dtm-m5.sh
  # → Uncovered NFR: 0
  ```

- [ ] `scripts/check-dtm-m6.sh` 未対応 BR-BUS 0 件
  ```bash
  bash scripts/check-dtm-m6.sh
  # → Uncovered BR-BUS: 0
  ```

---

## ALCOA+ 検証確認

- [ ] TST-alcoa-001（Attributable）: `work_events.resource IS NULL` = 0 件
- [ ] TST-alcoa-002（Legible）: veraPDF 3b exit code = 0
- [ ] TST-alcoa-003（Contemporaneous）: P95 sync_lag_ms ≤ 2000ms
- [ ] TST-alcoa-004（Original）: PostgreSQL トリガ UPDATE 拒否確認
- [ ] TST-alcoa-005（Accurate）: 境界値 HTTP 400 + ERR-VAL-002 確認
- [ ] TST-alcoa-006（Complete）: XES 必須属性 NOT NULL 確認
- [ ] TST-alcoa-007（Consistent）: `verify_chain()` PASS + 破壊的テスト broken_at 検出
- [ ] TST-alcoa-008（Enduring）: veraPDF 3b PASS + Noto フォント埋込確認
- [ ] TST-alcoa-009（Available）: quality_admin HTTP 200 / operator HTTP 403 確認

---

## UAT 合格確認

- [ ] TST-001〜007（全判定レベル）全項目 PASS
- [ ] UAT 実施記録（UAT-YYYYMMDD-Vx）が作成されていること
- [ ] FR-AU-001 準拠電子サインオフが完了していること
  - UUID v7 が発行されていること
  - HMAC-SHA256 署名が記録されていること
  - signer_id が記録されていること
  - timestamp_server が記録されていること
- [ ] ALCOA+ 9 原則全 Pass（TST-alcoa-001〜009）
- [ ] NFR-QUA-003 倫理品質自己監査チェックリスト全 PASS

---

## ドキュメント完全性確認

- [ ] CHANGELOG.md に ver1.0.0 エントリが記載されていること
- [ ] `docs/07_テスト/適格性確認テスト/証跡/` に証跡ファイルが保存されていること
- [ ] UAT 実施結果 PDF が `docs/07_テスト/適格性確認テスト/証跡/` に保存されていること

**本節で確定した方針**
- **本チェックリストの全項目にチェックが入った状態でのみ `08_リリース判定とサインオフ.md` に進むことを確定する。**
- **RGATE-001〜006・ALCOA+ 9 件・UAT 合格 4 条件のいずれか 1 項目でも未達の場合は ver1.0.0 リリースを保留する。**
- **本チェックリストはリリース都度複製・日付付きファイル名で `証跡/` に保存する。**

---

## 参照業界分析

- `../../../90_業界分析/06_品質管理とトレーサビリティ.md`（必須）
- `../../../90_業界分析/22_規制別トレーサビリティ要件詳論.md`（必須）
- `../../../90_業界分析/11_計測・工程能力と統計的品質工学.md`（関連）

---

## 版数履歴

| バージョン | 日付 | 著者 | 変更内容 |
|---|---|---|---|
| 0.1.0 | 2026-05-18 | RyuheiKiso | 初版 |
