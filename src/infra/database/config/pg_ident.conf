# pg_ident.conf — PostgreSQL ユーザー名マッピング設定
# 権威ドキュメント:
#   ADR-012（PostgreSQL 16 採用）
#
# 本システムでは OS ユーザー名と PostgreSQL ユーザー名のマッピングは使用しない。
# pg_hba.conf での認証は scram-sha-256 パスワード認証を使用するため、
# ident マッピングは不要である。
#
# 形式: MAPNAME  SYSTEM-USERNAME  PG-USERNAME
#
# （エントリなし）
