{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:wnav:schema:external_key:v1",
  "title": "ExternalKey",
  "description": "外部システム（ERP/MES）識別子のキーバリューマップ。TBL-027 external_key_bindings.external_key に保存する。キー名は外部システムに依存するため固定スキーマを持たない。値は全て文字列型とする。GIN インデックス（IDX-013）により @> 演算子での部分一致検索が可能。",
  "type": "object",
  "minProperties": 1,
  "additionalProperties": {
    "type": "string",
    "minLength": 1,
    "maxLength": 256,
    "description": "外部システム識別子の値（文字列型のみ許可）"
  },
  "examples": [
    { "lot_id": "L001", "product_code": "P-A001-REV2" },
    { "work_order_no": "WO-2026-001234" },
    { "lot_id": "L002", "serial_no": "SN-00123" },
    { "erp_material_no": "MAT-00456", "plant": "1000", "storage_location": "0001" }
  ]
}
